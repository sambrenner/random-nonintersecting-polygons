{
  "name": "random-polygon",
  "version": "1.0.0",
  "description": "generates a random non-intersecting polygon",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --out-dir docs",
    "build": "rm -rf docs && parcel build index.html --out-dir docs --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@svgdotjs/svg.js": "^3.0.16",
    "hull.js": "^1.0.0",
    "noisejs": "^2.1.0",
    "simplify-js": "^1.2.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
